1. Just discovered (GPT told me) there exist programs that talk directly to the GPU (js talks to CPU) which accelerate the rendering of pixels on screen. One of them is WebGL, which I intend to learn with time.

Check this resource: https://webglfundamentals.org/

- GPU := state machine (buffers...)
- gl.X constants
- Describe pipeline

2. https://maximmcnair.com/p/webgl-film-grain

3. Check kind of noises
